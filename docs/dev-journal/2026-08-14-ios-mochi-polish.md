# Log: iOS Mochi polish + fix pass

### Prompt
"im working on this ios app with hermes but its messing up some stuff. i want all the functionality of the portal we have rn but how it looks needs to be based on mochi app... go through the entire portal then the ios app and then start refining things and animations." Scope confirmed: polish + fix pass, whole staff team as audience (real roles).

### Issue
The app did not build (and therefore never updated in the simulator): `SetterActivityDetail.swift` existed on disk but the XcodeGen-generated pbxproj was never regenerated after the file was added (concurrent Hermes session). Beyond that: two duplicated slide-over sidebar implementations, ad-hoc corner radii, hardcoded founder role, broken `myRoles()` decode, hardcoded Home greeting/date, dead code in Performance, `MoneyInView` fed a constant binding inside a nested ScrollView, Payments screen unreachable, DM-setter KPI judged at the retired 125/3 targets, almost no haptics/matched-geometry animation.

### What I did
- Regenerated the Xcode project (`xcodegen generate --spec project.yml`), restoring a clean build; installed and relaunched on the Ivy Portal iPhone 17 Pro simulator.
- Added shape tokens (`ivyRadiusCard/Tile/Row/Icon`) and moved SurfaceCard/StatTile/Skeleton onto them.
- Extracted one shared `SlideOverMenu` + `SlideOverRow` (Components.swift) replacing the duplicates in PortalShell and CustomersView; sidebar GO TO rows now land on the correct Work tab, and a new Payments row makes the Payments surface reachable for the first time.
- Real roles: fixed `PortalAPI.myRoles()` decode (rows, not bare strings), AuthStore now loads roles + home views on sign-in/session restore, PortalShell derives tabs and the release Home picture from `RoleDestinationPolicy`/`HomePicturePolicy` instead of hardcoded `[.founder]` (signed-out DEBUG preview keeps founder).
- Home greeting/date now clock-driven instead of hardcoded strings.
- Performance: deleted dead `realTeam`/`MenuRow`, fixed deprecated `onChange`, single fetch path for day-range changes, removed fake-tappable empty stat-card closures everywhere (19 call sites across 4 files).
- Work hub: `MoneyInView` gets a real binding and composes inside the hub scroll (nested ScrollView removed).
- Team Week KPI now mirrors web `didHitKpi`: sets-first OR full volume, DM 125·3 before 2026-07-29 and 300·6 after, `max(dms_sent, leads_contacted)` outreach, full-cycle 100 dials AND 50 DMs; removed dead `todayLine`.
- Animations/haptics: matchedGeometryEffect on the floating tab bar, SegmentedPicker, Performance section/range chips, Work-hub tab chips; `.sensoryFeedback` on tab/section/menu/source changes; `.contentTransition(.numericText())` on hero/stat values; `.presentationCornerRadius(28)` on all main sheets; reduce-motion respected on new animation paths.

### How I did it
Explored web portal + iOS app with parallel agents first, planned, then edited: `Components.swift`, `PortalShell.swift`, `CustomersView.swift`, `AuthStore.swift`, `PortalAPI.swift`, `HomeView.swift`, `PerformanceView.swift`, `SetterActivityDetail.swift`, `TeamWeekView.swift`, `OperationsViews.swift`, `PortalSurfaces.swift`. Built via `DEVELOPER_DIR=/Applications/Xcode.app/... xcodebuild` (xcode-select points at CommandLineTools). Verified: clean build, 20 SPM tests + IvyPortalTests all pass, screenshots of home/menu/performance/work/customers/payments checked against the Mochi reference shots.

### What was challenging
- Diagnosing "no changes in simulator": the pbxproj is a generated artifact with explicit file references, so a new file silently breaks every subsequent build until xcodegen re-runs. Rule going forward: always regenerate after adding/removing files.
- Concurrent Hermes session: two Phase-0 fixes (UI test label, map-shadow hack) had already landed by the time I read the files; committed nothing to avoid clobbering its WIP (working tree left uncommitted, pathspec-scoped commits recommended).
- KPI logic drift between web and iOS: ported `didHitKpi` semantics exactly rather than inventing native behavior (business rule, founder-locked).

### Round 2 (same day, founder feedback batch)

Feedback: sidebar GO TO was "nonsense"; Work had "a million tabs"; icons/sizing misaligned; "See all" dumped into Work; nav bar bubble ignored text size, "Performance" truncated, AI circle too small/misaligned; wanted Google sign-in.

Done: GO TO removed (sidebar = VIEWS only); WorkTab consolidated to Actions/Calendar/Money/My EOD/Chat/Knowledge (Set tracker inside Calendar, Expenses inside Money via MoneyInTab.expenses, CRM + redundant Payments summary deleted; HomeAction routing + tests updated); nav bar rebuilt Mochi-style (active squircle wraps icon+label, "Performance" → "Stats" with chart.bar.fill, 64pt AI circle center-aligned); StatTile fixed 96pt height with one-line scaled labels and bottom-pinned values; activity feed "See all" expands in place, overlap badges in a fixed 52pt frame so text columns align; "Volume yesterday" redesigned to value/target progress rows; dead taps swept (FunnelRow disabled+chevron-less without action, empty fixture closures stripped, Submit my EOD wired to Work → My EOD, Payments "Match" stubs replaced with honest captions); Google sign-in via Supabase OAuth + ASWebAuthenticationSession (`ivyportal://` scheme in generated Info.plist; requires `ivyportal://auth-callback` added to Supabase Auth redirect allowlist); More screen shows real roles; pull-to-refresh on Home/Performance/Clients; bottom fade under the floating bar; AI sheet upgraded to a real entry surface; sidebar swipe-to-dismiss.

### Round 3 (autonomous loop while founder was out)

Two subagent reviews drove this round. Code review findings fixed: ASWebAuthenticationSession now retained (continuation could hang sign-in forever), role fetch retries on every foreground, Performance range-switch stale-response guard, StatTile back to minHeight (Dynamic Type clipping), TeamWeek hitKPI returns false for unset setter type (matches web). UI audit findings fixed: Work chip row auto-scrolls to the active chip (was invisible for Knowledge/Chat), Knowledge/Chat flattened into the hub scroll (double 40pt inset gone) with dead chevrons removed, More no longer shows a Sign out nav row alongside the sign-in card, Calendar card is real-date-driven (was hardcoded "12 August Wednesday" over a 1-7 strip), "received" color noise fixed, "Missed yest" → "Missed yesterday" (UI test updated), "Need/Needs attention" copy unified, duplicate "Weekly Report" header removed. Also: deleted unreachable PayoutsView/FinanceView/CardsView + helpers and dead DailyStrip, presentationCornerRadius(28) on all remaining sheets, AI sheet redesigned as a real entry surface, sidebar swipe-to-dismiss, pull-to-refresh on Home/Performance/Clients, bottom fade under the floating bar. Full test suite (20 unit + 6 UI tests) green.

### Round 4 (second-pass review fixes)

A second review agent on the round-2/3 diff found 7 issues, all fixed: signed-out founder fallback now DEBUG-only (release strangers no longer saw leadership surfaces); floating chrome (tab bar + AI circle + bottom fade) hides via a SlideOverActiveKey preference whenever any slide-over menu is open, fixing the Clients menu rendering under the fade; the Home view-switcher burger is DEBUG-only until picture switching ships live (release rows were dead); WebAuthenticator releases only its own session reference; a roles refresh that removes the active tab now sends the user Home instead of stranding them; dead `.expenses` fixture case cleaned; TeamWeek ISO date parsing pinned to POSIX/Gregorian for Saudi devices. Full suite re-run green; final build installed on the simulator.

### Round 5 (nav bar redo against the Mochi keyframe)

Founder re-flagged the nav bar: active bubble ignored label width, "Performance" name + chart.bar.fill icon disliked, AI circle same size as the bar and misaligned; instructed to copy Mochi's bar exactly. Rebuilt `floatingTabBar`/`tabItem` against `keyframes/07-overview-loaded` in the design reference: cells now hug content (padding-based bubble around icon+label, so the squircle follows the label width; matchedGeometryEffect kept), pill uses hugging cells with 3pt gaps instead of equal columns, icon language is Mochi's outline-at-rest/filled-when-active (`symbol(active:)`), Work checklist → checkmark.circle, Stats chart.bar.fill → chart.line.uptrend.xyaxis, and hover shape now matches the bubble (`contentShape(.hoverEffect)` + `.hoverEffect(.highlight)`) for iPad/Mac pointers. AI circle raised to 74pt (bar pill is ~63pt, so it pokes past both edges like Mochi's avatar) with a 29pt sparkles glyph and `.lift` hover. Scrubbed the visible "Performance" name: page header (`PerformanceSection.weeklyReport.title`), error copy, Home "Setters this week" link, sign-in card. Verified via simulator screenshots against the Mochi keyframe; 20 unit + 6 UI tests green (one strip test flaked on suite launch timing, passed twice on rerun).

### Round 6 (four founder feedback batches, copied from his Mochi screenshots)

Batch 1 — activity feed: badge pair rebuilt to Mochi's layering (translucent tinted event-icon circle behind-left, solid gray avatar ON TOP right, ~38% overlap, no black rings), "Latest Activity" header moved INSIDE the card, sub-line column-aligned under the main line via one text VStack, separators dropped for spacing, and "See all" now opens a full-height Activity sheet (centered title, X circle, wrapping sub-lines, 8 fixture events; `-showActivity` launch arg presets it). Batch 2 — nav bubble: active tab is a 21pt-radius near-circular squircle hugging icon+label inside a Capsule pill with 7pt concentric inset (no more cut-off first bubble); AI circle 76pt. Batch 3 — sidebar: `SlideOverMenu` replaced by a `slideOverMenu` push-drawer modifier (content + tab bar slide right ~314pt, drawer owns the left column, tap-content/swipe closes); the burger travels with the content and morphs to an X (`\.portalMenuActive` + symbolEffect replace) in PortalShell (title "Home", rows Sales/Fulfillment/Leadership/Personal, bare glyphs, no subtext/kicker) and CustomersView (title "Clients"); shell chrome now only fades for CHILD drawers. Batch 4 — More tab → Settings (gear tab icon, outline/fill): Mochi-style grouped list Admin/Profile/Organization/Team/Tags/Content Hub/Socials/Integrations/App with white glyphs + chevrons + inset separators, red Log Out card when signed in; `MoreEntry` reshaped in IvyPortalCore with gating admin→admin rows, FOUNDER-only Content Hub/Socials (admin does not unlock), Team row hosts TeamAdministrationView, Profile sheet shows the account block. Also: stat-card chips all continuous-corner squircles sized to the card's curve family (SplitMetricCard 36/12, compact 26/9, drilldown 58/19, Payments 48/15), SplitMetricCard titles shortened ("Messages sent"/"Replies received") with real title-chip gap, compact tile labels no longer truncate; Home spacing pass (table card padding 12, divider indent 56, section spacing 28); DEBUG signed-out preview roles now [founder, admin]. Tests: 21 unit + 9 UI green (3 new UI tests: activity sheet, push drawer + X morph, Settings list; policy test for founder-only settings).

### Round 7 (batches 5–9: per-person Home, bar geometry, margins sweep)

Per-person Home shipped: `HomePicturePolicy` now leads every leader with the general Overview (`.leadership` renamed in all UI to "Overview", founder: "the main view whenever anybody goes to Home"), granted Sales/Fulfillment views follow as drawer extras, non-leaders get Personal. Release `HomeView` finally renders pictures with LIVE data only: Overview = EOD-coverage hero (big number + progress) + Active students/Calls booked tiles + command queue; Sales = sets/shows/closes/coverage tiles + live setter `EntityRow` table from `performanceSummary(7)`; Fulfillment = phase-mix `BreakdownBar` computed from the roster (legacy `coaching_1on1`→training) + delivery tiles; Personal = portal-style reminders card (Submit today's EOD → new `HomeAction.openMyEOD` routing to Work→My EOD + action items). Picture selection is wired end-to-end: `PortalShell.homePicture` exists in release, defaults to the policy's first picture after roles load until the member picks one (`pictureChosen`), drawer lists only `availablePictures`, burger shows in release only when >1 view. DEBUG default/preview order now leads with Overview.

Nav bar (two more rounds): founder first asked for a perfect-circle highlight with smaller icons; after his "no margins between text and pill" follow-up with a hi-res Mochi crop, the bar was rebuilt to measured Mochi geometry: 58pt cells (fixed 55pt width), ONE 54pt circle nearly pill-height marking the active tab with the 12pt label inside its lower arc, 17pt icons, bright (0.9/1.0) labels, capsule pill with 2-3pt insets, 68pt AI circle (their 65:57 ratio).

Margins sweep (founder: "this universally should never happen"): every `EntityRow` list card at `SurfaceCard(padding: 6)` (Clients ×5, Team admin ×2) bumped to 14 and Home's row tables 12→14; divider indents unified at 56 (avatar 42 + gap 14). Activity badges: both circles now the SAME translucency (tint 0.24 / white 0.15) so the overlap reads as a soft union, neither layered above the other. Concurrent-session note: a parallel session was live-editing `SplashView.swift` (bun mascot splash) and transiently broke the build with a `.exit`→`.exitPhase` rename mid-edit; waited it out rather than touching their WIP — earlier "bao screen" screenshots were their new splash, not a crash. Tests: 21 unit + 9 UI green throughout.

### Round 8 (dual activity feeds + fine-tuning)

Badge overlap founder-tuned to spacing -8 across three nudge rounds. Activity feed split into TWO Mochi-identical cards (founder: "copy this word for word... latest setter activity and CSM activity"), picture-scoped: Sales → "Latest Setter Activity" (lead motion, setter assigned, EODs, installments), Fulfillment → "Latest CSM Activity" (CSM notes, Start Here finished, phase moves, 1:1 bookings, testimonial collected), Overview → both, Personal → neither. Shared `ActivityFeedCard` + titled `ActivitySheet`; avatar = subject, mini icon = actor. Nav pill vertical padding 2→4 so labels clear the border. UI tests updated (setter sheet asserts, CSM feed on fulfillment). 21 unit + 9 UI green.

### Round 9 (bar micro-tuning + Work cleanup)

Bar tuned across five founder nudges: pill larger than its unchanged cells, highlight 62→61pt (fits "Settings" via bigger arc + 11pt label — his own suggestion), highlight-to-pill margin settled at ~5pt, AI circle 67pt fitting inside the pill height. Work hub thinned per "way too many tabs": Team chat REMOVED (web keeps it); Calendar and Upcoming merged into ONE card (date hero + week strip + divider + upcoming rows); the Money tab REMOVED from Work — the money surface (MoneyInView, renamed "Payments" everywhere visible since he dislikes the word "money") now presents as a leader-only sheet opened from the money tiles on Home pictures (`HomeAction.opensPayments`; tiles only exist on leader pictures, so setters can never reach it — role rule preserved). Work is down to Actions / Calendar / My EOD / Knowledge. Routing tests rewritten for the sheet; new UI test taps Cash collected → Payments sheet. 21 unit + 10 UI green.

### Round 10 (founder overhaul batch: Work/EOD, Actions, Settings surfaces, Stats data, Clients)

Work: chip order now End of day (renamed from My EOD) / Calendar / Actions / Knowledge, default tab End of day; the EOD tab is the web form translated (week pulse tiles, MY WEEK strip with red missed days + dashed today, midnight-own-timezone deadline note, KPI progress with working DM/phone Switch type, steppers, submit with local state, MY LAST 7 DAYS with ✗/✓); calendar hero compacted so Set tracker is closer. Actions: overdue is RED, filters founder-gated, and Add opens a real composer (custom text; assign all students/one student/all team/one member; due chips; member-item visibility footnote) — visibility rule recorded in memory (member items = founders + assignee only; student items = everyone). Home Personal picture = web setter home (My week red strip, streak + rank lines, Open EOD, last-7-days, action items row). Settings: Team is the Mochi page (owner card, All/Setters/Closers, member rows with Pending, member detail sheet with role pill + Email/Status/Commission + Deactivate/Remove, fixture-local); Organization supports MULTIPLE orgs with add + switch (multi-tenant ambition); Content Hub is the Mochi staff library (search, All/Links/Media/Templates/Prompts, his real template shortcuts, + composer) and its gating moved to ALL STAFF (distinct from web founder-only content planning — policy + tests updated); Admin lists its coming tools honestly. Stats: drilldown day chips now scale color intensity by value share of the week's peak (Mochi-style), per-metric daily series + teammate splits that sum EXACTLY to each card total, TeamWeek empty-past-day = red "no EOD" (orange stays missed-KPI), Active hours bars thinned (9pt fixed width), SetterActivityDetail gained a "Both" source summing Mochi+Close, replies detail explains rate/median. Clients: Students tab = web roster translated (count line, phase chips with live counts incl. At risk, 1:1/Group pathway filter, rows with health score badges + phase/coach/pay/calls-left + phase pills), CSM tab = web overview (6-tile strip with success rate vs 95% target, quiet-2-weeks, first wins; Student health and Where-students-stand breakdown bars; coverage queue), 1-on-1 gained a working Add-call sheet. 21 unit + 10 UI tests green throughout; verified by simulator screenshots.

### Round 11 (web-portal sweep + corrections + Home revamp)

An Explore agent mapped the entire web codebase (migrations > types.ts > DATABASE.md; types.ts is STALE — missing archived_at, portal_activity, setter_daily_logs, kpi_targets, eod_exempt and more). Key truths applied to iOS: `student_calls` is a PAST LOG with per-call `coach_id` (status auto-derives from date; completed requires 1–5 `progress_rating`; only completed counts against `calls_allotted`; CSMs see but cannot insert) — the 1-on-1 tab now shows calls-used + a Recent-calls log ("with {coach}") and a Log-call sheet (student → who was this call with → past-date calendar → REQUIRED star rating → outcome → notes). EOD form gained wins (REQUIRED)/blockers/tomorrow-focus per the `eods` columns and lock rule. Requests tab now has Approve-as-Student / Team-member actions (web `pending_signups` flow). Profile shows the load-bearing timezone. NEW: notifications bell on Home (DEBUG fixture) mirroring the web bell's computed triggers (payout unconfirmed, overdue installment, signup, stuck-in-Start-Here, no-student-EOD, interview-soon) with a badge and routing sheet — the web has NO notifications table; it's all client-computed. HOME REVAMP from the web's actual composition: Overview = money strip + card + Operating brief as ROWS (active students/calls 7d/payments 3d/testimonials) + urgent-first Next-actions queue; Sales = web tile order (sets, show rate, volume-vs-target, cash this week, closes, pipeline, UNCLAIMED SETS) + setter table with showed/no-show splits; Fulfillment = paired tiles interleaved with phase bar, coldest-check-in rows, CSM table, notes. Sweep report also captured: four distinct at-risk formulas (do NOT unify), health score weights (35/15/15/15/20, bands 70/40), `owesEods()`, KPI eras via `kpi_targets`, semi-monthly payout shapes, HARD_GATES route ceilings. Tests: 21 unit + 10 UI green (one test updated for the renamed Cash-this-week tile).

### Round 12 (Phase E2 founder surfaces + two feedback nudges)

Phase E2 of the big refinement plan landed (see `2026-08-14-big-refinement-plan.md` for the full inventory): live notifications bell, Payouts tab with the exact web ledger math, live Deals tab with Log-a-close, live Plans tab with the money writes. Two founder nudges handled the same day: the "My card" tile moved from the top of Home Overview to the bottom of the picture ("remove ... or move it to the bottom a bit"), and the Students roster rows were slimmed after his "too messy / convoluted / overwhelming" screenshot — one caption line (pathway, coach only when a 1:1 student has one, pay fact), phase stated only by the pill, calls-left moved to the detail sheet, "Coach Unassigned"/"Coach –" filler deleted, and group rows no longer show any coach (that also closed a group-never-shows-1:1 rule violation in the fixture). List-row rule recorded in memory: never restate in caption text what a pill already shows, never render filler for absent data.

### Round 13 (2026-08-15, E3 batch 1: refinements + data fixing)

"Continue with more refinements and polishing and data fixing." Landed: live Knowledge (docs + reader), the student health score ported with tests and applied to a fixture-parity live roster (health badges, live-count chips, pathway filter, slim captions), student MANAGE writes (phase/coach/archive with the group-no-coach rule enforced and roster reload on dismiss), the real money strip on live Overview (logged cash month + truth-first left-to-pay-out), and live profile editing incl. the load-bearing timezone with a searchable picker. Data fixes: "Need attention/At risk" now counts the health formula's red band instead of a status=="ghosting" guess; the profile sheet previously displayed the DEVICE timezone as if it were the account's; raw dates leaking on student detail and doc rows now go through the new `ivyFriendlyDay()` (web friendlyPastDay port).

### Round 14 (2026-08-15, TestFlight readiness: full portal sweep + data parity fixes)

Founder asked to push to TestFlight after a full sweep. An exhaustive web-inventory agent produced a route-by-route parity checklist; verified against production Supabase (project `ivy-portal-eu` = the app's `hlfeshzkfbzkvlullqnb` — same accounts, 31 students / 22 deals / 146 EODs / 62 role grants live; all 60+ columns the app touches exist; `pending_signups` + `refund_student_money(p_archive)` present).

Data-parity fixes shipped from the sweep's findings: (1) `eods` closer money columns (`deposits`, `cash_collected`, `deferred_cash`) added to EODSubmission AND the form is now role-shaped (setter/closer/CSM sections per held roles — closers filing from phone no longer record $0 cash; CSMs get looms/roleplays/check-ins/escalations steppers). (2) `student_calls` writes now carry `action_items_json` (structured rows the student portal RPC toggles), `next_call_date`, `fathom_url` — call sheet gained action-items lines, next-call toggle+date, Fathom link. (3) `archiveStudent` also sets `status: "inactive"` (web sets both; status-filtered reads must agree). (4) NEW `student_checkins` write via a Log check-in row on student Manage — phone check-ins now move the founder's coverage tiles (csm_tally alone never did). (5) CSM note composer on the student Notes tab (`csm_student_notes` with `user_id` author) — notes render verbatim on the founder's fulfillment Home. Also: live action-item rows now humanize due dates and no longer count due-today as overdue (timestamp vs day-string compare bug).

Release readiness: RELEASE builds now open on AuthView when signed out (TestFlight first-run = login screen; DEBUG keeps the design preview); CalendarView no longer shows "Sign in" copy to signed-in users (clock-driven hero/strip always real; fixture rows DEBUG-only; honest caption in release); all 13 `loadError ?? "Sign in to…"` fallbacks neutralized; found and fixed a ship-blocker where `ComposedAction`/`ComposerPerson`/`ActionComposerSheet` lived inside `#if DEBUG` while live paths used them (Release had NEVER compiled since E1). Both Debug and Release configurations now build.

TestFlight scaffolding: final bundle id `com.ivysalesacademy.ivyportal` (tests `.tests`/`.uitests`), `ITSAppUsesNonExemptEncryption=false`, 1024 single-size AppIcon verified, `ExportOptions.plist` (app-store-connect + upload destination), `scripts/testflight.sh` (regenerate → archive with automatic signing + timestamp build number → upload via ASC API key), `TESTFLIGHT.md` with the founder's one-time steps (Apple Developer enrollment/Team ID, ASC API key, Supabase redirect allowlist `ivyportal://auth-callback` for Google sign-in; email+password needs nothing). No signing identity exists on this machine, so the upload itself waits on those credentials.

Production security hardening (applied + repo migration `20260815060000_relock_refund_and_ping_grants.sql`): `CREATE OR REPLACE` of the p_archive refund variant had silently restored anon EXECUTE on `refund_student_money` (and `portal_ping` shipped open); revoked from PUBLIC/anon, kept authenticated/service_role; verified via `has_function_privilege`. Remaining advisor notes (not actioned): `eods_activity*` SECURITY DEFINER views are the web's intentional read layer; leaked-password protection is a dashboard toggle for the founder.

Sweep gaps deliberately NOT blocking TestFlight (ranked in the parity report): real calendar + set reminders (server-fn mediated), team chat, CSM today-queue/coverage surfaces, card spend logging (`wallet_entries`), global search, closer payment links, testimonials upload, plan editor, `role_access`/`hide_money` honoring (flagged: web-hidden pages are visible on phone; RLS still protects rows).

### Round 15 (2026-08-15, one-home rule + Reports rename + gap batch 1)

Founder: "I don't want to have different views myself" — ONE home per person, no switching: sales view role = Sales homepage (Abu Bilal), fulfillment = Fulfillment (Faizan), founder (no view role) = general Overview, non-leaders = Personal. `HomePicturePolicy.pictures` now returns exactly one picture (supersedes the 2026-08-14 Overview-first rule; memory updated); the release burger disappears by itself (count is never >1). Also renamed the tab he disliked twice: "Stats" → **"Reports"** with doc.text icons (page header, error copy, Home link all swept).

Gap batch 1 (from the parity sweep, in his priority order): **Real sets in Calendar** — MY SETS live rows (prospect, friendly time, 48h/24h/3h/1h window chips that tick `reminder_log`, Confirm stamping `confirmed_at`; owner-scoped RLS confirmed to allow direct updates — claiming stays web-side because its server flow syncs Google Calendar) + UNCLAIMED awareness list. **CSM coldest-first queue** ("Who needs me today") on the CSM tab: days-since-check-in from `student_checkins`, one-tap check-in with optimistic tick. **Closer payment links** in Knowledge (active `payment_links`, one-tap copy). **Founder wallet card** in Payments Overview (`wallet_entries` credits−spends balance + Log spend / Load card sheets — the swipe-moment write). **role_access foundation**: `RoleAccessPolicy` in Core (exact nav-pages port: configurable roles only, every-held-role-must-hide union rule, grants capped by HARD_GATES, admin/founder exempt; 3 unit tests + hard-gated pages default to their gate) — applied to Work chips, and `hide_money` now masks Deals/Plans figures ("•••") via `maskedMoney`. **Closers can finally reach Log-a-close from the phone**: a Personal-home row opens the Payments sheet on Deals (they had no path before — leader tiles were the only entry). AuthStore loads `role_access` with roles and clears it on sign-out.

### Round 16 (2026-08-15, finish batch before TestFlight)

The last signed-in fixture surfaces are gone: **Testimonials is live** (status-chip list over real rows, detail sheet with signed-URL image preview + copy-viewing-link, camera-roll capture via PhotosPicker → storage `testimonials/{studentId}/{uuid}.{ext}` + `received` row exactly like the web, text/Trustpilot entries, request-a-testimonial flagging; 60MB cap with an honest overflow message; approval stays web-side). **Requests is live**: `pending_signups()` queue with Approve-as-setter/closer/coach/csm (the web's plain `user_roles` insert); approve-as-student stays web (its server flow creates the student + payment setup) and says so. **Global search** shipped (web command-palette, phone-shaped): magnifier next to the Home bell → one box over students (name/email → student detail), teammates (roles caption), and docs (→ reader). **EOD draft autosave**: every field persists per user+day in UserDefaults, restores on return, clears on submit — a call mid-form loses nothing. **Student detail depth**: WEEKLY REPORTS section (student_weekly_eods: group calls + 1:1 count, implementation, win/blocker) and a PLACEMENTS card (business, stage pill, interview time); daily-EOD dates humanized. Debug + Release build; suite green.

### Round 17 (2026-08-15, founder batch: exact routing, EOD data truth, all SOPs, table cleanup + Phase F)

Founder feedback (screenshot of the Set tracker daily log + voice note): messy wrapping table with "$2.500" money, tiles landing on general pages instead of the exact data element ("that's with everything"), EOD DM numbers still wrong vs the portal, Knowledge missing the portal's SOPs ("can't open any of this stuff").

Fixes: (1) **Daily log table**: friendly one-line dates via `ivyFriendlyDay`, new shared `ivyMoney()` pins ALL money to en-US ("$2,500" never "$2.500"), fixture dates now rolling real dates. (2) **Exact-element routing (recorded as universal rule)**: `HomeAction.clientsTab` lands every fulfillment tile on its Clients section (CSMTab moved to Core, hoisted to PortalShell); at-risk tiles preselect the roster's At-risk filter; sets/show-rate/coverage tiles open Reports directly ON the live EODs section (`startOnEODs`, tab-bar taps still land on Weekly Report); money tiles already preset their Payments tab. (3) **EOD form web parity**: setter type is now the PROFILE field (phone/dm/full_cycle) read/written via `profiles.setter_type` with the web's pick-prompt and cycle button; DMs sent + Convos started show for EVERY setter type (only Dials is DM-hidden); submission records every entered field as-is (the old form ZEROED a phone setter's DMs and a DM setter's dials — the "DM numbers not correct" root); booked>convos warning added; full-cycle targets 100 dials + 50 DMs + 3 sets inline. (4) **EOD tab data truth**: "Your recent reports" and the MY WEEK card now read the member's real `eods` rows (new `PortalAPI.myEODs`) — the old code read `setter_daily_logs` (Mochi set tracker), so submitted EODs never appeared; week strip/streak/KPI-hit/leads are all computed from the real rows (web didHitKpi shape), fake "#3 of 6" rank line deleted. (5) **All portal SOPs bundled**: `IvyPortal/Resources/SOPs/` ships the two Foundations policies (EOD & Meetings, CRM Hygiene — extracted verbatim from the policy pages), Setting Process (all 88 script cards + guided workflow converted from sections.tsx), Simple Discovery Framework, both Objection playbooks (raw transcripts sectioned with the web's exact sliceByMarkers labels), and World-Class Client Delivery Systems (orphaned on web; surfaced under CSM); `BundledSOPs` registry mirrors web role gates; Knowledge renders Foundations + Core SOPs even when the docs table is empty/unavailable (the founder's "can't open anything" was an empty docs read); reader upgraded with blockquote script styling, real bullets, table fallback. (6) **Phase F items landed**: EOD-due chip on Home (real `owesTodayEOD` check: filing roles minus founder/eod_exempt, day in profile timezone, clears on submit), AI-circle long-press quick actions (Log EOD / Log a call / New action item with composer pre-open), Work/Clients tab-icon long-press sub-tab menus, StudentDetail chips matched-geometry + selection haptics, auth sheet corner radius, stepper accessibility labels, launch-args documented in `ios/README.md`.

### Future work
- EOD submit form (first write path), AI chat backend for the navbar sparkle circle, real data for Payments/Payouts/Cards (+ `payouts-calc.ts` port), student surfaces, UI test run on CI, `.refreshable` pull-to-refresh, adopt `LoadState` enum instead of hand-rolled value/loading/error triples, restyle PerformanceDetailSheet day chips onto `DailyStrip`.
