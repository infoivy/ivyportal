---
projectDir: /Users/x/ivyportal/ios
description: The Big Refinement Plan — fixtures everywhere, Settings completion, AI identity, EOD compaction, portal parity (writes), QOL, optimization, animation system
gitBranch: main
status: active
---

# Log: Ivy Portal iOS — Big Refinement Plan

Full plan: `/Users/x/.claude/plans/make-a-massive-massive-delightful-pinwheel.md` (approved 2026-08-14).
Founder decisions: real Supabase writes immediately in E1 (no preview flag); motion feel = playful but restrained (mascot pull-to-refresh + sparse micro-delights).

## Status

- [x] Phase 0 — Correctness: is_demo/voided/archived filters on all PortalAPI reads; role-gate contract comment
- [x] Phase A — Fixture everywhere (set tracker + benchmark bands, expenses, student output, testimonials/requests live-stub swap, PreviewBadge unification ×13, DemoFixtures.swift + 6 components + PaymentsTab deleted, PaymentsView folded into Payments sheet as Clients/Costs tabs)
- [x] Phase B — Settings completion (Tags library + composer, Socials snapshot, Integrations status rows w/ live Supabase state, App info + portal link; PortalDetailSheet corner radius)
- [x] Phase C — AI identity (BunLogo avatar in nav circle + sheet header; real TextField composer, tappable suggestion chips, honest footer)
- [x] Phase D — EOD compaction (one merged week card, lastSevenDays deleted, KPI targets inline on steppers, type-gated steppers, deadline as caption — ≈1990pt → ≈1000pt)
- [x] Phase E1 — Write path: EOD submit → `eods` insert (23505 = locked, error surfaced), action items create (broadcast = many rows) + optimistic done-toggle, 1-on-1 call log → `student_calls` (coach fallback = current user), CSM tallies → `csm_tally` with long-press undo. Set actions DEFERRED (server-fn-mediated on web; needs endpoint check before native writes)
- [x] Phase E2 — Founder surfaces: PayoutsCalc.swift = exact port of payout-period.ts/payouts-calc.ts/revenue.ts (semi-monthly halves, co-founder $1k/period + $2k/month caps chronological, $5k Mon–Sun week +1%, self-set 15%, base-pay day anchoring + full-month eligibility; 16 unit tests). Payments sheet gained a Payouts tab (admin/cofounder, mirrors web page gate; period nav, hero, per-member Mark paid → payout_confirmations upsert + undo → delete, adjustments composer → payout_adjustments) — money tiles now preset their sheet tab (openPayouts → Payouts, revenue → Deals, installments → Plans; HomeAction.moneyTab + tests). Deals tab live (recent deals + Log a close: student create/link, deal insert, even/custom installment builder, non-admin closers locked to self). Plans tab live (mark paid / waive with reason / refund payment / refund_student_money RPC with receipt; paid history immutable). Live bell: PortalAPI.portalAlerts computes the web bell's 6 families natively (payout-unconfirmed via the real ledger math, pending_signups RPC, unclaimed sets, set nudges 48/24/3/1h + keep-warm, student alerts incl. Start Here stuck/fresh-unlock/EOD/payment-behind/1:1 cadence/interviews ≤48h, installment reminders ≤3d with coach scoping) with web role gates, sort, and red/amber badge tones; NotificationsSheet is live for signed-in users. Money strings pinned to en-US ($1,478, never "US$ 1.478" on Saudi locales). Same-day founder feedback: My card tile demoted to Overview bottom; Students roster rows slimmed to one caption line (pathway + coach-only-when-real + pay; phase only in the pill; calls-left moved out; group rows never show a coach). Tests: 39 package + unit green, 12 UI green (2 new: bell sheet routing, payouts tile → ledger tab).
- [~] Phase E3 — Depth (2026-08-15 batch 1): Knowledge LIVE (docs read with RLS role scoping, pinned→sort→updated order, search over title/content/category, category groups, markdown reader sheet with external links + block renderer). Student health = exact `student-health.ts` port in IvyPortalCore (weights 35/15/15/15/20, locked/graduated branches, ghosting cap 25, bands 70/40; 11 unit tests) + `studentHealthMap()` running the web's five-read aggregation; live roster upgraded to fixture parity (health badges, phase chips with live counts incl. At risk = red band, pathway filter, slim founder-approved caption; At-risk stat replaces the old status=="ghosting" count — data fix). Student detail gained MANAGE (canManage = admin|coach|csm): phase picker, coach picker (1:1 students only — group never shows coach), archive with confirm; roster reloads on sheet dismiss so archived students never linger. Live Overview money strip is REAL now (web home-money-strip port: cash collected this month = logged deals upfront + paid installments; left-to-pay-out = current period owed minus confirmed, truth-first allPaid state). Profile editing live: display_name/phone/timezone (searchable IANA picker, device-zone shortcut; timezone decides the EOD day) — replaced a block that wrongly displayed the DEVICE timezone as if it were the profile's. New `ivyFriendlyDay()` (web friendlyPastDay port) fixes raw-date leaks (student detail Last EOD, doc updated lines). Still open in E3: student timeline/installments tabs, testimonials upload, role grant/revoke writes, live role_access honoring.
- [x] Phase E3 remainder (2026-08-15 batches 2–3): role_access + hide_money honored (Core policy + tests, Work chips, money mask), testimonials live + camera-roll upload + request flow, requests live + approve-as-team, weekly EODs + placements on student detail, real set reminders in Calendar (tick/confirm; claim = web), CSM coldest-first queue + student_checkins write, payment links, wallet card (log spend/load), global search, EOD draft autosave, closer log-a-close path. Team role grant exists via Requests approve; deeper roster/team writes (revoke, deactivate) stay web-first with the Team admin page.
- [x] Phase F (2026-08-15, interleaved with a founder feedback batch): EOD-due Home chip (live owesTodayEOD, clears on submit), AI-circle long-press quick actions (Log EOD / Log a call / New action item incl. composer pre-open), tab-icon long-press sub-tab jumps (Work + Clients), launch args documented in ios/README.md, auth-sheet corner radius, StudentDetail chips matched-geometry + haptics, stepper/star accessibility labels verified. Same batch (founder-directed): exact-element tile routing everywhere (clientsTab + At-risk preset + Reports lands on EODs section), EOD form profile setter_type + all-fields-submit parity, myEODs-backed week card/history, all portal SOPs bundled into Knowledge, ivyMoney() en-US sweep on the daily log.
- [ ] Phase G — Optimization pass
- [ ] Phase H — Animation & loading system
